#include <iostream>

int main() {
    std::cout << "Starting SLOW client" << std::endl;

    // loop that receives input to to allow functions:
    // connect
    // send data
    // disconnect

    return 0;
}