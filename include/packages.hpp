#pragma once 


// definir o UUID como classes - parte do pacote slow, mas por ser grande definiremos separado


// definir o pacote slow classes
// - definir metodos de preenchimento do pacote
// - definir metodos de serialização e desserialização
// - serializaçao -> dado um obj, retorna um array de bytes
// - desserializaçao -> dado um array de bytes, retorna um obj

// flags:
// C R A A/R MB (connect, revive , ack, accept/reject, more_bits)